# Synapse + Claude Code

Keep project `CLAUDE.md` thin and point it to `EXECUTOR.md`.

Keep personal or local Claude instructions if you already use them.
Synapse governs execution, not identity.

For governed work:

- start from repo `CLAUDE.md`
- open `EXECUTOR.md`
- use Synapse runtime commands and wrappers for governed operations

Persona overlays are optional.
If Claude already has working personal instructions, keep them.
